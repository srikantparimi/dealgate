{
  "name": "dealgate-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end suite for DealGate (blueprint § 11 scenarios).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
