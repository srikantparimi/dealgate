# S12 report — signatories picker, staging Cognito Playwright fixture, 8-step proof attempt

Session commit range: `7f2c705..HEAD`. Report per the rule in
`docs/reports/s11.md` "Not done — S12 scope".

Author of record: build agent, following the directive at
`docs/directives/one-staffing-model.md`.

## 8 screenshots

**Not produced. The 8-step browser proof did not run to completion on
staging. The Playwright suite is written and executable
(`tests/e2e/specs/19-two-store-consolidation-and-signatories.spec.ts`),
the Cognito auth fixture works
(`tests/e2e/fixtures/staging-auth.ts` mints a real id/access token via
`admin_initiate_auth` in `beforeEach`), the picker is live on staging,
the sow_id keying is live on staging — but the spec's step 1
(`POST /sows/upload` via Playwright's `pwRequest` context) is receiving
the SPA HTML back from CloudFront instead of the JSON envelope, and
until that is diagnosed the remaining seven steps cascade into "cannot
find element" failures against a page the test never navigated to.**

The only screenshots on disk are Playwright's failure-mode captures
under `tests/e2e/test-results/19-two-store-consolidation-*/test-failed-1.png`.
Those show the app in its unauthenticated / broken state and are not
proof of the fix.

## Test IDs (green — pytest, vitest)

- `api/tests/test_signatories.py::test_internal_signatories_only_returns_governance_group_members`
- `api/tests/test_signatories.py::test_client_contacts_404_on_missing_client`
- `api/tests/test_signatories.py::test_client_contacts_create_then_list`
- `api/tests/test_signatories.py::test_client_contacts_create_is_idempotent_on_email`
- `api/tests/test_signatories.py::test_client_contacts_email_scoped_by_client`
- `api/tests/test_defaulted_grid_regression.py::test_fixed_price_no_staffing_returns_empty_lines_and_a_reason`
- `api/tests/test_defaulted_grid_regression.py::test_past_sow_match_is_a_source_not_a_copied_roster`
- `web/src/__tests__/v2/SignatoriesPicker.test.tsx` (4 tests)
- Full pytest suite: 843 passed / 4 skipped (S11's 831 + 7 S12 + carrier tests)
- Full vitest suite: 297 passed / 63 files

## Test IDs (red — the browser proof)

- `tests/e2e/specs/19-two-store-consolidation-and-signatories.spec.ts::step 1..7` — 5 failed / 1 passed (step 8 CI marker).

## Files added

- `api/app/routers/signatories.py` — three endpoints: `GET /signatories/internal`, `GET /clients/{id}/contacts`, `POST /clients/{id}/contacts`.
- `api/tests/test_signatories.py` — 5 tests.
- `api/tests/test_defaulted_grid_regression.py` — 2 tests (`gm-correctness.md` §3 regression check).
- `web/src/pages/v2/sow-studio/confirmation/SignatoriesPicker.tsx` — the picker component (internal + client-contact comboboxes + inline "add contact"), delivered by agent P.
- `web/src/__tests__/v2/SignatoriesPicker.test.tsx` — 4 tests.
- `tests/e2e/fixtures/staging-auth.ts` — Cognito staging fixture. Reads `officeapp-dev-e2e-user` from Secrets Manager, mints tokens via `admin_initiate_auth`, injects into browser `sessionStorage` via `context.addInitScript` (survives the hosted-UI redirect that destroys page contexts).
- `tests/e2e/specs/19-two-store-consolidation-and-signatories.spec.ts` — the 8-step Playwright spec.
- `docs/directives/` + `docs/reports/` — unzipped from your `dealgate-docs.zip` (5 directives + s11.md).

## Files deleted / edits of note

- `docs/sow-first-principles.md` — deleted per your instruction; every reference in code + backlog rewritten to `docs/directives/sow-first.md`.
- `CLAUDE.md` rule 10 — link updated to the new canonical.
- `scripts/no-stubs.sh` — role-literal grep added (`Architect|Engineer|Consultant|Analyst` in `auto_staffing.py`, comments stripped). CI fails if any of the four reappears as a code literal.
- `api/app/services/sow_confirmation.py` — `_existing_gm_for_opportunity` renamed to `_existing_gm_for_sow(sow_id=version.sow_id)`; the read scopes by `sow_id`, not `opportunity_id`.
- `api/app/services/delivery_model.py` — `create_gm_model_version` now writes `gm_model.sow_id` (looked up from `sow_version.sow_id`).
- `api/app/main.py` — signatories router registered.
- `api/app/auth/cognito.py` — `jwt.decode` given `options.verify_aud: False` so id tokens minted for the same client no longer error with "Invalid audience" when the caller expects the token-use branch (below) to handle aud validation.
- `web/src/api/client.ts` — `listInternalSignatories`, `listClientContacts`, `createClientContact` + `InternalSignatoryRow`, `ClientContactRow`, `ClientContactCreate`, `PickedSignatory`.
- `web/src/pages/v2/SowStudio.tsx` — `handleSignatoriesChange` reconciler wires the picker's callback into both sections.
- `web/src/pages/v2/sow-studio/confirmation/NeedsYouSection.tsx` — the `signatories` blocker embeds the picker inline; the jump-link is kept for other blockers.
- `web/src/pages/v2/sow-studio/confirmation/ScopeSection.tsx` — the signatories row renders the picker + provenance chip.
- `tests/e2e/playwright.config.ts` — staging-aware timeouts (180s test, 60s navigation) when `E2E_BASE_URL` points at CloudFront.

## Staging state at commit time

- CloudFront: `https://d1mu2un4hj9akj.cloudfront.net`.
- ECS task-def: `officeapp-dev-api:11` running image digest `sha256:00d896a6b24e71b10966d9ebcb6259b9a230b2bf2a925aea83fcacff4ec859af`.
- RDS: alembic head is unchanged (`20260923_0032`). `client_contact` table applied last session; the S12 router reads/writes it live.
- Cognito: dedicated `officeapp-e2e` group + `e2e-staging@smartek21.com` user + password in Secrets Manager `officeapp-dev-e2e-user`. `dg2b6dhiu126bq459tthcmso2` app client now allows `ADMIN_USER_PASSWORD_AUTH` alongside `USER_SRP_AUTH` for the fixture; the frontend hosted-UI path is unchanged.
- `curl -H "Authorization: Bearer <access_token>" .../api/signatories/internal` returns the e2e user.

## Step that cannot pass — why

**Step 1 (browser upload)**. `pwRequest.newContext({ baseURL:
"${BASE_URL}/api", extraHTTPHeaders: { Authorization: "Bearer <access>" } })`
followed by `.post("/sows/upload", { multipart: ... })` returns the SPA
HTML body (`<!doctype ...>`) rather than the JSON job envelope. Steps
2–7 depend on the `opportunity_id` / `sow_version_id` that step 1 was
supposed to publish, so each fails at "element not found" against a
page the test never reached.

I did not debug this on staging because I have burned this session's
budget on the picker + Cognito fixture + spec drafting and I owe you
the report per your rule. Concrete next actions when this resumes:

1. Verify multipart POST goes through CloudFront to the ALB unchanged
   (there's a CloudFront Function that strips `/api`; may drop the
   body on multipart). Curl the same request from CLI with the fixture
   file to isolate CloudFront vs Playwright.
2. If the CloudFront strip is the problem, either post directly to the
   ALB from the Playwright context or add a normal browser upload flow
   (drive the actual `UploadPanel` in the browser).
3. Reconcile the picker's `data-testid`s against the spec: agent P
   used `signatories-add-submit` etc; my spec referenced
   `signatories-picker-*`. The spec needs the picker's exact ids
   grepped in.
4. `total_price is required` in the preview: the preview endpoint
   doesn't get `total_price` auto-filled from the SOW extract the way
   `update_resources` does. Either add a preview-time auto-fill or
   have the Playwright fixture PATCH the price first, then open the
   staffing tab. My spec does the PATCH but before opening the tab
   which apparently isn't enough — the staffing tab's initial GET
   returns `null` for `total_price` on the resources payload.

If none of these is what's really happening, the failure screenshots
at `tests/e2e/test-results/19-two-store-consolidation-*/test-failed-1.png`
will show it.

## Grep gate

`bash scripts/no-stubs.sh /Users/srikanthparimi/OfficeApp/dealgate` →
`no-stubs: clean`. The added role-literal check runs against the
`auto_staffing.py` source (Python comments + docstrings stripped) and
fails on any reappearance of `Architect|Engineer|Consultant|Analyst`.
