# S13a report — delete + reset, portal proof, and the three defects

Directive: `docs/directives/s13a-delete-reset.md`. Runs against
`https://d1mu2un4hj9akj.cloudfront.net` on ECS task-def
`officeapp-dev-api:24`, image tag `s13a-devseed-v8`
(`669810405473.dkr.ecr.us-east-2.amazonaws.com/officeapp-dev-api:s13a-devseed-v8`,
digest `sha256:e4379f320a4ed6e862a0226c23aad54e54b095e61829ef143ecbb3f21fb44a8a`).
RDS alembic head `20260924_0033_archived_at` (adds `archived_at/by/reason`
on `client` / `opportunity` / `sow` + partial "live" index on Postgres).

## Portal proof (Playwright, staging)

Spec: `dealgate/tests/e2e/specs/20-s13a-delete-and-archive.spec.ts` —
**5 tests / 5 passed** against the deployed CloudFront origin. Full log at
`/tmp/s13a-full-v6.log`. Screenshots in `docs/reports/s13a/`, each linked
below.

| # | Portal step | Screenshot | Directive |
| --- | --- | --- | --- |
| 1 | Fresh SOW upload → new client is on the pipeline with an S13a run tag | `01-fresh-client-on-pipeline.png` | §DoD 2 |
| 2 | Row-menu → Delete… → cascade confirmation with counts (`opportunit…` visible) | `02-cascade-confirmation-modal.png` | §DoD 1 |
| 3 | Confirm delete → dialog closes → list empty for the tag | `03-list-empty-after-delete.png` | §DoD 1 |
| 4 | Same-file re-upload after delete → picker → clean create with S13a "Reupload" tag | `04-reupload-clean.png` | §DoD 2 |
| 5 | Approved package on a client → row-menu Delete… → Archive-instead button, Delete gone | `05-approved-refuses-delete.png` | §DoD 4 |
| 6 | Click Archive → dialog closes, row leaves default pipeline view | `06-archived-row-hidden-from-defaults.png` | §DoD 4 |

The spec's `afterAll` invokes `cleanupClientsByPrefix(BASE_URL, RUN_TAG)`
which now falls through to `POST /api/dev/purge-client/{id}` when the
governance-checked DELETE returns non-2xx (approved / archived). The
last run reported `deleted=2 skipped=0` — **zero residue** on staging,
directive §DoD 6.

## API proof (curl, staging) — carried forward from the earlier session

```
$ DELETE /api/clients/7d8e…?reason=…  → 200
$ POST   /api/sows/upload  (same bytes as deleted)
  {"job_id":"…","status":"needs_pick","duplicate":false}
$ POST   /api/sows/jobs/…/pick    → {"status":"done","opportunity_id":…}
$ POST   /api/sows/upload  (SAME bytes, no delete)
  {"status":"done","duplicate":true,"opportunity_id":…}
$ GET    /api/clients?search=peppermill
  owners:[{id:"…", name:"E2E Staging Bot"}]  (§2.2 fixed)
  sources:["sow_upload"]                     (§2.3 fixed)
```

## What shipped this closure

### Files added

- `api/app/routers/dev_seed.py` — **new test-only endpoint**
  `POST /api/dev/seed-approved-package/{opportunity_id}`. Gated on
  `DEALGATE_ENV in {local,test,dev,staging}` AND
  `ALLOW_DEV_SEED_ENDPOINT=1` AND caller must be SystemAdmin. Short-
  circuits the S12 signatories + submit ceremony for the browser proof.
  Full multi-role journey stays as backlog
  `docs/backlog/e2e-approval-journey.md`.
- `web/src/ui-v2/DeletionConfirmationDialog.tsx` — cascade-counts +
  Archive-instead dialog.
- `web/src/__tests__/ui-v2/DeletionConfirmationDialog.test.tsx` — 4
  tests (draft cascade + Delete, approved → Archive-instead + server
  reason, hubspot_linked banner, 4xx surfaces inline and keeps dialog
  open).
- `web/src/__tests__/v2/Pipeline.test.tsx` — 2 tests (kebab trigger
  visible for admin; hidden for a plain Sales user).
- `docs/reports/s13a/*.png` — six screenshots above.
- `docs/backlog/e2e-approval-journey.md` — placeholder for the full
  four-role approval e2e (Delivery → HR → Finance → Legal → CEO).

### Files changed this session

- `api/app/routers/dev_seed.py` — endpoint gains explicit GM-seed +
  `{ok:false, ...}` on every failure so CloudFront's CustomErrorResponses
  cannot silently rewrite a 4xx to `/index.html`.
- `web/src/pages/v2/Pipeline.tsx` — row kebab + Delete… entry, role-
  gated via `getIdTokenClaims`.
- `web/src/pages/ClientDetail.tsx` — top-right "Delete client" button;
  navigates to `/pipeline` on success.
- `web/src/pages/v2/BulkSowImport.tsx` — "Delete batch" button next to
  "Run again"; navigates to `/settings/imports` on success.
- `web/src/api/client.ts` — `assessClientDeletion`, `deleteClient`,
  `archiveClient`, `assessOpportunityDeletion`, `deleteOpportunity`,
  `archiveOpportunity`, `deleteBulkImportBatch`,
  `DeletionAssessmentResponse` type.
- `tests/e2e/fixtures/staging-auth.ts` — `cleanupClientsByPrefix`
  helper (used by `20-s13a-*.spec.ts::afterAll`).
- `tests/e2e/specs/20-s13a-delete-and-archive.spec.ts` — 5-test suite:
  fresh upload, row-menu delete, reupload, duplicate, seeded-approved
  refuses delete + offers archive.
- `api/app/services/user_provisioning.py` — `ensure_user` now hydrates
  Cognito attributes when the access token doesn't carry email/name and
  the DB row's fields look like UUIDs (was: one-off SQL patch).

### Files changed this closure (fresh-start rule + cascade fix)

- `api/app/services/client_resolver.py` — **archived-aware scoring**
  (Kanna refinement 21 Sep 2026). Two scoring buckets: live clients (the
  only ones eligible to auto-match) and archived clients (surfaced as
  `ResolutionResult.archived_matches` + `info` string, never resurrected).
  Same pattern as the archived-SOW duplicate note in
  `sow_upload_job_service`.
- `api/app/services/deletion.py` — `_hard_delete_opportunity` now
  deletes `Approval` + `ApprovalPackage` **before** `GmModel`. Reason:
  `approval_package.gm_model_id` FK made the old order fail with
  `ForeignKeyViolation` on any opportunity that ever had a submitted
  package (surfaced by the dev-seed cleanup path).
- `api/app/routers/dev_seed.py` — new `POST /dev/purge-client/{id}`
  bypass endpoint used by the e2e cleanup helper to drop leftover
  approved+archived rows the normal DELETE endpoint (correctly) refuses.
- `tests/e2e/fixtures/staging-auth.ts::cleanupClientsByPrefix` — falls
  through to `/dev/purge-client/{id}` when the governance DELETE returns
  non-2xx. Guarantees `deleted=N skipped=0` in every run.

### Files unchanged from the earlier session (still shipping)

- `api/app/routers/deletion.py` — the seven delete/archive/assessment
  endpoints (from S13a-B2).
- `api/app/services/client_resolver.py` — possessive / entity-suffix
  canonicalisation (from S13a-B1, extended here).
- `api/app/services/clients.py`, `api/app/routers/clients.py` —
  `owners: OwnerRef[]` + `sources: string[]` on the list rows (S13a-B3).
- `api/app/services/sow_upload_job_service.py` — live-only hash dedupe
  (fresh-start rule).
- `api/alembic/versions/20260924_0033_archived_at.py`.

## Data changes on staging (recorded per Kanna's rule)

- **21 Sep 2026, closure**: one leftover `S13a e2e … · Approve` client
  (id `e179481d-7771-487f-b79f-2fef7dd17729`) survived earlier T5
  attempts (approved + already-archived — neither DELETE nor Archive
  would touch it). Purged manually via `POST /api/dev/purge-client/{id}`
  after the cascade-order fix landed in task-def :24. Cleanup helper now
  handles this class of leftover automatically going forward.
- The earlier one-off SQL fix (setting the e2e user's `email`/`name` to
  real values because the access token doesn't carry them) is
  **superseded** by the `ensure_user` Cognito lookup fix shipped in B4 —
  future runs won't need any manual data patch.

## DoD status

| # | Requirement | Status |
| --- | --- | --- |
| 1 | Row-menu Delete on Pipeline → cascade confirmation → confirm → row gone | **Portal-proven** (screens 01→02→03). |
| 2 | Re-upload same file after delete → clean, no duplicate flag | **Portal-proven** (screen 04); duplicate flag proven by API test. |
| 3 | Upload the same file a second time without deleting → duplicate flag | **Test T4** passes: `duplicate: true`. |
| 4 | Approved package → Delete refused → Archive offered → row leaves default view | **Portal-proven** (screens 05→06). Backend proved by `test_deletion.py` unit tests. |
| 5 | Matcher: "Peppermill Casino's, LLC" matches "Peppermill Casino" ≥ 0.85 | `test_client_resolver_normalisation.py` (14 tests) — 1.0 score. |
| 5b | Fresh-start: archived client does NOT resurrect on re-upload, and the match is surfaced as an informational note (not blocking) | `test_resolve_client_ignores_archived_and_notes_the_match`; resolver bucket splits live vs archived, `ResolutionResult.info` + `archived_matches` carry the note per Kanna's refinement. |
| 6 | E2E leaves zero residue on staging | `cleanupClientsByPrefix` in `afterAll` — last run `deleted=2 skipped=0`. Cleanup falls through to `/dev/purge-client/{id}` when the governance-checked DELETE refuses (approved / archived rows). |
| 7 | Existing tests green | pytest **875 pass / 4 skip** (postgres-only), vitest **306 pass** on the S13a code (the 1 failing vitest is `StaffingGmSection` on the S13b branch, not this slice). |

## Follow-ups (documented, not blocking)

- **CloudFront routing gotcha.** `POST /api/**` deep-path requests
  hitting a 4xx from ALB get rewritten by the distribution's
  `CustomErrorResponses` (403/404 → `/index.html`, code 200). This
  silently masks real backend errors when tools consume the response
  status alone. Two things follow from that:
  1. The dev-seed endpoint returns `{ok: false, status, error}` with a
     literal 200 on every failure, so the caller never mistakes a 200
     HTML fallback for success. Same fix should apply to any endpoint
     whose 4xx must be distinguishable from CloudFront's fallback.
  2. Terraform ADR follow-up: tighten `CustomErrorResponses` to the S3
     cache behavior only, or set `ErrorCachingMinTTL: 0` so failed POST
     origin responses aren't cached. Track in `docs/adr/`.
- **Full approval e2e journey.** `docs/backlog/e2e-approval-journey.md`
  tracks the full four-role browser walk-through (Delivery → HR →
  Finance → Legal → CEO). The dev-seed endpoint is the interim: it lets
  the delete-refusal + archive UI be exercised end-to-end without the
  four-role plumbing.
- **`ALLOW_DEV_SEED_ENDPOINT` terraform.** The env var was added
  directly to task-def :21 via `aws ecs register-task-definition`
  (drift-tolerated by the module's `ignore_changes = [task_definition]`).
  Fold into `infra-tf/modules/api/main.tf` when the next infra PR
  touches that module so the setting survives a `terraform apply`.

## Test IDs (green)

- pytest: `api/tests/test_client_resolver_normalisation.py`
  (**14 tests**, +1 for `test_resolve_client_ignores_archived_and_notes_the_match`),
  `api/tests/test_deletion.py` (8 tests),
  `api/tests/test_user_provisioning_cognito.py` (5 tests),
  `api/tests/test_dev_seed.py` (4 tests). Full suite
  **875 passed / 4 skipped** (postgres-only tests skipped in local).
- vitest: `web/src/__tests__/ui-v2/DeletionConfirmationDialog.test.tsx`
  (4 tests), `web/src/__tests__/v2/Pipeline.test.tsx` (2 tests).
  Full suite **306 pass / 1 fail** (the failure is
  `StaffingGmSection.test.tsx`, a pre-existing S13b branch test file
  outside this slice).
- Playwright: `tests/e2e/specs/20-s13a-delete-and-archive.spec.ts`
  **5 passed (1.0m)** against staging at
  `d1mu2un4hj9akj.cloudfront.net`; `S13a cleanup: deleted=2 skipped=0`.
