# S11 report — staffing-model consolidation (slices 1 and 2)

Reconstructed 20 September 2026 from the build agent's own two reports, verbatim in substance, and signed off by the product owner (Kanna Parimi) as the authoritative record. Commits: 233f9b4, 9a19c35, 7f2c705 on srikantparimi/dealgate. Product owner verified on staging: 2 SME lines render on Confirm with correct GM.

## Slice 1 (commits 233f9b4, 9a19c35)

Files deleted / removed code:

- `api/app/services/auto_staffing.py` — the entire hardcoded `[("Architect","Principal"),("Engineer","Senior"),("Engineer","Mid")]` roster (46 lines), plus the T&M "Consultant / Senior" fallback and the managed-service "Support Engineer / Mid" default.
- `api/app/services/sow_confirmation.py` — the `_existing_gm_for_sow(..., sow_version_id=version.id)` filter that orphaned every save that came in with `sow_version_id=NULL`. Replaced by a single sow-scoped query.
- `api/app/services/sow_confirmation.py` — the "auto-create GmModel from fabricated roster" branch. Auto-plan seeds an empty record only from SOW-supplied lines; a re-open never overwrites what a human saved.
- Frontend `saveDeliveryModelVersion` staffing path — `StaffingGate.tsx` now saves via `putSowStaffing` (`PUT /sows/{id}/staffing`, canonical).

On staging (ECS task-def :9): one staffing endpoint both pages call; `sow_version_id` auto-populated from the latest SOW version; `total_price` auto-filled from the extracted price on fixed-fee engagements; allocation renders 100% not 1.0000%.

## Slice 2 (commit 7f2c705)

Files added:

- `api/tests/test_staffing_byte_identity.py` (4 tests)
- `api/tests/test_no_phantom_seeds.py` (1 test)
- `api/tests/test_sow_id_scoping.py` (2 tests)
- `web/src/__tests__/v2/allocation-units.test.ts` (4 tests)
- `api/alembic/versions/20260923_0031_client_contact.py` (picker migration; applied on RDS)
- `api/alembic/versions/20260923_0032_gm_model_sow_id_backfill.py` (backfilled `gm_model.sow_id` on RDS)
- `api/app/models/client_contact.py`

Test IDs (all green; pytest 831 passed, web 293 passed):

- `test_staffing_byte_identity::test_saved_staffing_is_what_confirm_screen_shows` — save 2 SME × $120, reopen confirm → exactly 2 SME lines, provenance manual, byte-identical, no phantom.
- `test_staffing_byte_identity::test_reopen_confirm_never_reseeds_staffing` — 3 reopens, no reseed.
- `test_staffing_byte_identity::test_fixed_price_revenue_uses_extracted_price` — GM 42.4%, US floor passes.
- `test_staffing_byte_identity::test_raising_cost_triggers_ceo_gate` — line 2 → $220/hr → GM 10.4%, `requires_ceo: True`.
- `test_no_phantom_seeds::test_no_phantom_role_names_in_auto_staffing` — role-literal grep on `auto_staffing.py` → 0 hits.
- `test_sow_id_scoping::test_staffing_read_is_scoped_by_sow_id`
- `test_sow_id_scoping::test_approved_package_still_pins_gm_after_later_edit`
- `allocation-units.test.ts::formatAllocationPct` — "1.0000" → "100".

On staging (ECS task-def :10, RDS at alembic head 20260923_0032): store keyed by `sow_id`; approved-package pinning verified; no phantom roster path in production code.

## Not done — S12 scope

1. Signatories picker end to end: router, `SignatoriesPicker.tsx`, wiring into NeedsYouSection and ScopeSection, PATCH into `sow_version.extracted_fields.signatories`. Table and model exist.
2. Staging Cognito e2e fixture: `officeapp-e2e` group, test user, secret `officeapp-dev-e2e-user`, `admin_initiate_auth` in Playwright `beforeEach`. No credentials in the repo.
3. The 8-step browser proof from `docs/directives/one-staffing-model.md` with 8 screenshots, committed as `docs/reports/s12.md`.
4. CI grep for template role literals (`Architect|Engineer|Consultant|Analyst` in `auto_staffing.py`) added to `scripts/no-stubs.sh` or its own workflow step.
5. Regression check: a fixed-price SOW with no staffing table still gets a proposed, deletable, `defaulted` grid from the catalog — the phantom-roster deletion must not have removed the legitimate proposal path.
